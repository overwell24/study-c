## 포인터 주소 출력
```c
#include <stdio.h>
int main(void)
{
    int i=10;
    char c = 69;
    float f=12.3;

    printf("i의 주소: %p", &i);
    printf("c의 주소: %p", &c);
    printf("f의 주소: %p", &f);
    
    return 0;
}
```
